# AniMemo v1.1 重安全审计闭环检查点

日期：2026-08-18（Asia/Shanghai）

本文件是 Heavy Overnight Security Closure Sprint 的本地检查点，不是 Heavy Security Audit #1 的最终 PASS，也不是 Release/RC/部署授权。

## 权威边界

```text
BASE_MAIN_SHA=8421d1ed088f21b3f4a6ab190a496e0ab0b0df9f
BASE_MAIN_TREE=c04fbe29fbd61961e070df6fadc92937b237380e
BRANCH=chore/v1.1-pre-rc-consolidation
PREVIOUS_HEAD=6d8173097183b6f0164889fb81447db0e5fc8b26
PR=129
PR_FAST_GATE=PASS（仅适用于 PREVIOUS_HEAD）
REMOTE_LANE=BLOCKED_BY_GITHUB_OFFICIAL_OUTAGE
L3_AUTHORITY_AT_CHECKPOINT=684/685
HEAVY_SECURITY_AUDIT_1=PARTIAL
SECURITY_FULL_1_SHA=NOT_CREATED
FORBIDDEN_USER_FILE_READ=ZERO
```

GitHub 官方状态仍报告 API、Actions、Pull Requests 为 `major_outage`，Git 操作为 `degraded_performance`。本检查点没有调用 AniMemo 仓库 API，没有 push、rerun、PR 更新、Issue 更新或 gate 降级。

## 观察量与去重边界

审计证据根为 `E:\番剧记录\.animemo-security-audit-evidence\full1-8421d1e`，来源树为 BASE_MAIN_SHA。原始观察量必须保留，不因去重删除：

| 项目 | 数量 | 解释 |
| --- | ---: | --- |
| item-level 原始观察 | 1800 | L1=80、L2=7、L3=1659、SMOKE=1、CodeQL=53（来源计数可能重叠） |
| Vercel | 49 | 133/133 已分析，但当前只有 count-only/partial item-level authority |
| 合计观察 | 1849 | 不等于独立漏洞数 |
| preliminary cross-tool candidate groups | 28 | 覆盖 154 条 raw rows，仍非最终 ledger |
| preliminary root-cause clusters | 19 | 路径/关键词初分，不能单独冻结严重性 |

Vercel 的 49 条没有被伪造为逐项 finding。CodeQL Issue #128 的 53 条 `py/stack-trace-exposure` 保持 OPEN/PENDING_FOLLOWUP；已确认存在受控 domain error/validation message 与真实 traceback 混合的语义问题，不能机械批量关闭。

## 已完成的证据化处置

### 潜在 FP 集合

104 个 potential-FP 候选已经完成只读复核：

```text
CONFIRMED_FP=99
NOT_CONFIRMED_BEFORE_REMEDIATION=5
```

99 条的处置均有 source/entry/trust-boundary/sink/mitigation 证据，不以“看起来安全”作为理由。主要证据包括：测试模块不进入生产入口、installer materials 排除测试文件、受控一次性 fixture、认证/CSRF/能力边界、CAS/manifest 绑定、受信任 plugin runtime 契约及恢复保护模式的 fail-closed 行为。

### 五条残余候选与本地修复

以下五条是复核后未直接关闭的残余 ID；本地实现已完成，须在提交后的目标测试和新 SHA 权威 gate 上重新绑定：

| 原始候选 | 根因 | 本地处置 |
| --- | --- | --- |
| `SMOKE-HIGH-01` | workflow dispatch 数字输入在 Python 校验前进入 Bash 表达式 | 将 PR number 全部通过 step `env` 传递，Bash 只读取已绑定环境变量；保留 GitHub number/write 权限边界 |
| `UNCLECHENG_L1-0055`、`UNCLECHENG_L3-0594`、`UNCLECHENG_L3-0597` | performance fixture 使用固定 integration key/secret | 每次 seed 使用 CSPRNG 生成 key id/secret；probe 从已加密的当前 integration 读取 secret；新增轮换回归 |
| `UNCLECHENG_L3-1140` | load harness 允许任意明文 HTTP 目标并发送环境凭据 | 明文 HTTP 仅允许 loopback 或保留的 `example.test` 域；HTTPS 仍要求显式非生产目标 |

同一源码树链接穿越根因也已在 Bridge、pluginctl、official package 三条打包路径统一拒绝 symlink/junction/特殊文件，并补充负向测试。该修复保护的是 release/source-tree 边界，不引入 compatibility shim 或双重信任路径。

### 已检查的 L3 高危候选（非全局最终冻结）

在完整 L3 authority 尚未结束前，以下处置只表示 exact-SHA source evidence，不表示全局审计 PASS：

- OAuth state、restore member path、CI output canonicality、plugin archive member confinement、plugin route authorization、updater operation id：`PRELIMINARY_FALSE_POSITIVE_WITH_SOURCE_EVIDENCE`。
- private store race：当前权限模型下为 `PRELIMINARY_FP_OR_P3_HARDENING`，未发现较低权限攻击链。
- official package source links：真实 gap，攻击者需要控制受信 release checkout，归为 `PRELIMINARY_P2_SUPPLY_CHAIN_HARDENING`，已纳入本地源码树 guard。
- plugin trusted-runtime egress、plugin storage caller trust：取决于“trusted in-process”长期契约；若未来契约承诺 host-enforced egress，必须 `FROZEN_CONTRACT_REVIEW_REQUIRED`，不得用 helper shim 伪装隔离。
- storage endpoint SSRF：配置入口仅 superuser，归为 `PRELIMINARY_P2`，不是当前 unauthenticated/staff-capability P1。
- updater redaction key syntax / escaped JSON：未复现 secret 保留，归为 P3/潜在 FP，保留独立证据。

## L3 状态

`L3-0200-703e1db6ed27` 已按既定 subdivision 合法闭合：A/B/C 与 cross-subunit synthesis 全部 PASS，semantic profile/provider/model 与原 authority 一致，`BYTE_GAP=0`、`BYTE_OVERLAP=0`、`cross_context_gap=0`、`fallback=0`。A/B 复用既有结果，仅对 C 做一次受控重试；v2 C timeout 取证保留。证据位于 `E:\番剧记录\.animemo-security-audit-evidence\full1-8421d1e\siliconflow-l3-failed13-recovery-v1\final-two\l3-0200-subdivision-run-v3`。

`L3-0362-ff4dcf12bc61`（`durability/secret_envelope.py`）仍是唯一已知的 security authority blocker：已知一次 provider long-tail timeout，禁止盲目重试、升 timeout、换 provider/model 或不安全 subdivision。该文件本轮未修改；最终 authority 必须重新验证该单元。

因此当前不能写入 `L3_FULL=PASS`、`HEAVY_SECURITY_AUDIT_1=PASS` 或 `SECURITY_FULL_1_SHA`。

## 本轮源码与测试变更

产品/工具变更：

- `scripts/package-astrbot-bridge.py`：Bridge source tree root/entry link guard。
- `scripts/pluginctl.py`：plugin source tree root/entry link guard。
- `backend/plugin_host/official_packages.py`：official package source tree link/special-file guard。
- `backend/performance/seed.py`、`backend/performance/probe.py`：performance integration credential per-run rotation。
- `scripts/perf/load_harness.py`：plain-HTTP isolated-target allowlist。
- `.github/workflows/pre-merge-full.yml`：dispatch PR number 通过环境变量进入 shell。

回归覆盖：

- `scripts.tests.test_security_tool_paths`：7 tests，Windows symlink capability 不可用导致 2 skipped，其余 PASS。
- `scripts.tests.test_perf_load_harness`、`scripts.tests.test_ci_authority_workflows`：合计 26 tests，PASS。
- `plugin_host.tests.test_pluginctl`、`plugin_host.tests.test_official_sync`、`journal.test_performance_baseline`：31 tests，PASS；Windows symlink negative test 3 skipped，SQLite historical ZIP SHA 1 skipped。

## 当前 gate 裁决

```text
REVIEWED_CONFIRMED_P0=0
REVIEWED_CONFIRMED_P1=0
GLOBAL_P0_P1=NOT_FROZEN_UNTIL_REMAINING_CRITICAL_HIGH_LEDGER_AND_L3_AUTHORITY
SMOKE_HIGH_01=LOCALLY_HARDENED; REMOTE_WORKFLOW_REVALIDATION_PENDING
ISSUE_128=OPEN/PENDING_FOLLOWUP
PHASE4_STATUS=PASS
TD_P0_UNRESOLVED=0
TD_P1_UNRESOLVED=0
RELEASE_DRAFTER_STATUS=READY
CANDIDATE_SECURITY_DELTA=PENDING_POST_COMMIT
PR_MERGED=NO
PRODUCTION=NOT_RUN
RELEASE=NOT_RUN
```

## 必须完成的剩余事项

1. 保持 L3-0362 为单一 authority blocker，不做无证据 PASS；不创建新的 685-unit Full。
2. 收敛尚未覆盖的 raw Critical/High ledger，区分 root-cause duplicate、confirmed FP、lower severity 与真实 unresolved P0/P1。
3. 对本轮修复运行目标测试、relevant L1/L2 revalidation 和 base→candidate security delta；不得把“未发现”写成全局 PASS。
4. 本地提交使用中文 commit message。GitHub 恢复后等待 5–10 分钟稳定窗口，验证 remote PR HEAD 与本地新 SHA 完全一致；只为新 SHA 运行缺失的 `pr-fast-gate`/`pre-merge-authority`，不复用旧 SHA authority。
5. Heavy Audit #1 仍为 PARTIAL 时不 merge、不创建 RC/tag/release、不部署。

本检查点的成功标准是：保留全部原始证据、减少真实安全关键路径、提供可复验的源码与测试结果，并明确剩余 authority blocker；不以降低 gate 换取 PASS。
