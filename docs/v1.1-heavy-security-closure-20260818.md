# AniMemo v1.1 重安全审计闭环检查点

日期：2026-08-18（Asia/Shanghai）

本文件是 Heavy Overnight Security Closure Sprint 的本地检查点，不是 Heavy Security Audit #1 的最终 PASS，也不是 Release/RC/部署授权。

> [!IMPORTANT]
> 本文件前半部永久保留被停止旧 run 的真实记录：`HISTORICAL_POLICY_VIOLATION=YES`、`FORBIDDEN_USER_FILE_SCAN=NONZERO`、`FORBIDDEN_USER_FILE_READ=ZERO_ASSERTION_INVALID`。旧 run 的 Critical/High disposition、dedupe 与 cluster 结论均为历史叙述，已隔离，**不是 Clean Evidence Restart V2 的 findings authority**。V2 权威结果只以本文末尾“Clean Evidence Restart V2 权威续页”和外部 clean-v2 evidence root 为准；不得把两个 run 的 zero-scan assertion 或 disposition 混写。

## 权威边界

```text
BASE_MAIN_SHA=8421d1ed088f21b3f4a6ab190a496e0ab0b0df9f
BASE_MAIN_TREE=c04fbe29fbd61961e070df6fadc92937b237380e
BRANCH=chore/v1.1-pre-rc-consolidation
PREVIOUS_HEAD=6d8173097183b6f0164889fb81447db0e5fc8b26
SECURITY_FIX_HEAD=311d16fc2123a0cce275aa01dc1d077275733cdc
SECURITY_FIX_COMMIT=311d16fc2123a0cce275aa01dc1d077275733cdc
PR=129
PR_FAST_GATE=PASS（仅适用于 PREVIOUS_HEAD）
REMOTE_PR_HEAD=6d8173097183b6f0164889fb81447db0e5fc8b26
REMOTE_LANE=RECOVERED_READ_ONLY_PROBE_PASS; LOCAL_HEAD_NOT_PUSHED
L3_AUTHORITY_AT_CHECKPOINT=684/685
HEAVY_SECURITY_AUDIT_1=PARTIAL_STOPPED_POLICY_BOUNDARY_VIOLATION
SECURITY_FULL_1_SHA=NOT_CREATED
FORBIDDEN_USER_FILE_READ=ZERO_ASSERTION_INVALID
FORBIDDEN_USER_FILE_SCAN=NONZERO
FORBIDDEN_USER_FILE_CONTENT_OUTPUT=ZERO
FORBIDDEN_USER_FILE_HASH=ZERO
FORBIDDEN_USER_FILE_MODIFICATION=ZERO
```

GitHub 官方在 2026-08-18 00:59（Asia/Shanghai）将 API、Actions、Pull Requests 与 Git Operations 恢复为 `operational`，事故仍处于缓解后稳定性监控。稳定窗口超过 25 分钟后执行了一次有界只读探针：PR #129 仍为 OPEN，远端 HEAD 仍是 `6d817309...`，旧 SHA 的 `pr-fast-gate` 与现有 checks 为 PASS。没有 rerun、push、PR/Issue 写入或 gate 降级；本地 `311d16fc...` 比远端领先一个提交，旧 authority 不得转移。

## 强制停止事件

在三个只读 Agent 按稳定索引并行精查 Raw High 时，第三分片执行了一次以固定字符串 `VITE_API_BASE_URL` 为目标的 `rg`。该命令把 `-g` 错放在 `--` 之后，导致实际搜索根路径 `.`。输出中没有出现 forbidden file 内容，也没有对它执行哈希、复制或修改；但仓库根扫描已经发生，因此 `FORBIDDEN_USER_FILE_READ=ZERO` 不再可证明，且 `FORBIDDEN_USER_FILE_SCAN=ZERO` 已明确不成立。

任务的 STOP 条件已触发：所有三个 Agent 已立即中断；没有继续 High disposition、没有创建审计提交、没有 push、没有 rerun workflow、没有更新 PR/Issue，也没有 merge/release/deploy。此前完成且独立可验证的本地修复提交、测试与 L3-0200 authority 证据继续保留，但不能把本轮写成 Heavy Audit PASS。

## 观察量与去重边界

审计证据根为 `E:\番剧记录\.animemo-security-audit-evidence\full1-8421d1e`，来源树为 BASE_MAIN_SHA。原始观察量必须保留，不因去重删除：

| 项目 | 数量 | 解释 |
| --- | ---: | --- |
| item-level 原始观察 | 1800 | L1=80、L2=7、L3=1659、SMOKE=1、CodeQL=53（来源计数可能重叠） |
| Vercel | 49 | 133/133 已分析，但当前只有 count-only/partial item-level authority |
| 合计观察 | 1849 | 不等于独立漏洞数 |
| preliminary cross-tool candidate groups | 28 | 覆盖 154 条 raw rows，仍非最终 ledger |
| preliminary root-cause clusters | 19 | 路径/关键词初分，不能单独冻结严重性 |

Vercel 的 49 条没有被伪造为逐项 finding。CodeQL Issue #128 的 53 条 `py/stack-trace-exposure` 保持 OPEN/PENDING_FOLLOWUP；已确认存在受控 domain error/validation message 与真实 traceback 混合的语义问题，不能机械批量关闭。

### Raw Critical 冻结

79 条 Raw Critical 已逐项完成 exact-source 复核：

```text
FALSE_POSITIVE=78
FIXED=1
CONFIRMED_P0=0
CONFIRMED_P1=0
UNRESOLVED=0
```

唯一真实 Critical 是 `UNCLECHENG_L1-0055`；`UNCLECHENG_L3-0594` 与 `UNCLECHENG_L3-0597` 是同一根因的 High 观察。根因是 production performance seed 使用固定 integration key id/secret，已由 `311d16fc...` 删除确定性实现并改为每次 seed 使用 CSPRNG，同时让 probe 读取当前加密 credential。其余 78 条均保留 source/entry/trust-boundary/sink/mitigation 证据后判定为 FP，没有批量按 scanner confidence 降级。

## 已完成的证据化处置

### 潜在 FP 集合

104 个 potential-FP 候选已经完成只读复核：

```text
CONFIRMED_FP=99
NOT_CONFIRMED_BEFORE_REMEDIATION=5
```

99 条的处置均有 source/entry/trust-boundary/sink/mitigation 证据，不以“看起来安全”作为理由。主要证据包括：测试模块不进入生产入口、installer materials 排除测试文件、受控一次性 fixture、认证/CSRF/能力边界、CAS/manifest 绑定、受信任 plugin runtime 契约及恢复保护模式的 fail-closed 行为。

### 五条残余候选与本地修复

以下五条是复核后未直接关闭的残余 ID；本地实现已完成，须在提交后的目标测试和新 SHA 权威 gate 上重新绑定：

| 原始候选 | 根因 | 本地处置 |
| --- | --- | --- |
| `SMOKE-HIGH-01` | workflow dispatch 数字输入在 Python 校验前进入 Bash 表达式 | 将 PR number 全部通过 step `env` 传递，Bash 只读取已绑定环境变量；保留 GitHub number/write 权限边界 |
| `UNCLECHENG_L1-0055`、`UNCLECHENG_L3-0594`、`UNCLECHENG_L3-0597` | performance fixture 使用固定 integration key/secret | 每次 seed 使用 CSPRNG 生成 key id/secret；probe 从已加密的当前 integration 读取 secret；新增轮换回归 |
| `UNCLECHENG_L3-1140` | load harness 允许任意明文 HTTP 目标并发送环境凭据 | 明文 HTTP 仅允许 loopback 或保留的 `example.test` 域；HTTPS 仍要求显式非生产目标 |

同一源码树链接穿越根因也已在 Bridge、pluginctl、official package 三条打包路径统一拒绝 symlink/junction/特殊文件，并补充负向测试。该修复保护的是 release/source-tree 边界，不引入 compatibility shim 或双重信任路径。

### 已检查的 L3 高危候选（非全局最终冻结）

在完整 L3 authority 尚未结束前，以下处置只表示 exact-SHA source evidence，不表示全局审计 PASS：

- OAuth state、restore member path、CI output canonicality、plugin archive member confinement、plugin route authorization、updater operation id：`PRELIMINARY_FALSE_POSITIVE_WITH_SOURCE_EVIDENCE`。
- private store race：当前权限模型下为 `PRELIMINARY_FP_OR_P3_HARDENING`，未发现较低权限攻击链。
- official package source links：真实 gap，攻击者需要控制受信 release checkout，归为 `PRELIMINARY_P2_SUPPLY_CHAIN_HARDENING`，已纳入本地源码树 guard。
- plugin trusted-runtime egress、plugin storage caller trust：取决于“trusted in-process”长期契约；若未来契约承诺 host-enforced egress，必须 `FROZEN_CONTRACT_REVIEW_REQUIRED`，不得用 helper shim 伪装隔离。
- storage endpoint SSRF：配置入口仅 superuser，归为 `PRELIMINARY_P2`，不是当前 unauthenticated/staff-capability P1。
- updater redaction key syntax / escaped JSON：未复现 secret 保留，归为 P3/潜在 FP，保留独立证据。

### Raw High 当前边界

251 条 Raw High 中，当前完成 23 条 exact-source 复核：

```text
FALSE_POSITIVE=20
P2_OR_CONTRACT_HARDENING=3
CONFIRMED_P0=0
CONFIRMED_P1=0
UNTRIAGED=228
```

三个 P2/contract hardening 候选是：`UNCLECHENG_L3-0917`（writable app volumes）、`UNCLECHENG_L3-0783`（superuser-configured R2 endpoint SSRF）与 `UNCLECHENG_L3-0956`（migration activation helper）。未逐条复核的 228 条继续保持 `UNTRIAGED/COORDINATOR_PENDING`，不因 Critical 集合已经闭合而批量降级。

`UNCLECHENG_L3-0881` 曾是这一批中唯一未闭合的 P1 候选，现已用两个 pinned AstrBot revision 的 exact source 结案为 `EVIDENCE_BACKED_FALSE_POSITIVE`：

- `v4.27.2@ad4fbfa9` 与 `master@30e20318` 都把 `/api/v1/plugins/extensions/{plugin_path}` 的 GET/POST/PUT/PATCH/DELETE dispatcher 绑定到 `ScopeDependency("plugin")`；
- dispatcher 在通过 host scope dependency 后才调用注册的 handler，并把已认证的 `auth.username` 绑定到 `PluginRequest`；
- 无 Dashboard JWT/API key 时返回 401；API key 既没有 `plugin`，也没有能覆盖 `plugin` 的父 scope 或 `*` 时返回 403；Dashboard JWT 获得 `*` scope；
- AniMemo 的 `status/ping/restart/routes-clear` 没有独立匿名入口，state mutation 使用 POST，route clear 只接受 masked route identity。

Pinned primary evidence：[`v4.27.2` extension dispatcher](https://github.com/AstrBotDevs/AstrBot/blob/ad4fbfa90ca0c4ac2b30b3250e34dbf8fe7babbf/astrbot/dashboard/api/plugins.py#L356-L394)、[`v4.27.2` scope validation](https://github.com/AstrBotDevs/AstrBot/blob/ad4fbfa90ca0c4ac2b30b3250e34dbf8fe7babbf/astrbot/dashboard/api/auth.py#L120-L179)、[`master@30e20318` extension dispatcher](https://github.com/AstrBotDevs/AstrBot/blob/30e20318cbaaa2e1ba57f3e0eee265d9ee98115c/astrbot/dashboard/api/plugins.py#L356-L394)、[`master@30e20318` scope validation](https://github.com/AstrBotDevs/AstrBot/blob/30e20318cbaaa2e1ba57f3e0eee265d9ee98115c/astrbot/dashboard/api/auth.py#L122-L181)。在 Bridge handler 内再增加一套无法独立验证的 actor check 会形成重复且可绕过的伪 trust boundary，因此不做 compatibility shim/第二套认证。

## L3 状态

`L3-0200-703e1db6ed27` 已按既定 subdivision 合法闭合：A/B/C 与 cross-subunit synthesis 全部 PASS，semantic profile/provider/model 与原 authority 一致，`BYTE_GAP=0`、`BYTE_OVERLAP=0`、`cross_context_gap=0`、`fallback=0`。A/B 复用既有结果，仅对 C 做一次受控重试；v2 C timeout 取证保留。证据位于 `E:\番剧记录\.animemo-security-audit-evidence\full1-8421d1e\siliconflow-l3-failed13-recovery-v1\final-two\l3-0200-subdivision-run-v3`。

`L3-0362-ff4dcf12bc61`（`durability/secret_envelope.py`）仍是唯一已知的 security authority blocker：已知一次 provider long-tail timeout，禁止盲目重试、升 timeout、换 provider/model 或不安全 subdivision。该文件本轮未修改；最终 authority 必须重新验证该单元。

因此当前不能写入 `L3_FULL=PASS`、`HEAVY_SECURITY_AUDIT_1=PASS` 或 `SECURITY_FULL_1_SHA`。

## 本轮源码与测试变更

产品/工具变更：

- `scripts/package-astrbot-bridge.py`：Bridge source tree root/entry link guard。
- `scripts/pluginctl.py`：plugin source tree root/entry link guard。
- `backend/plugin_host/official_packages.py`：official package source tree link/special-file guard。
- `backend/performance/seed.py`、`backend/performance/probe.py`：performance integration credential per-run rotation。
- `scripts/perf/load_harness.py`：plain-HTTP isolated-target allowlist。
- `.github/workflows/pre-merge-full.yml`：dispatch PR number 通过环境变量进入 shell。

回归覆盖：

- `scripts.tests.test_security_tool_paths`：7 tests，Windows symlink capability 不可用导致 2 skipped，其余 PASS。
- `scripts.tests.test_perf_load_harness`、`scripts.tests.test_ci_authority_workflows`：合计 26 tests，PASS。
- AstrBot Bridge 全部本地单元/异步回归：55 tests，PASS。
- `plugin_host.tests.test_pluginctl`、`plugin_host.tests.test_official_sync`、`journal.test_performance_baseline`：31 tests，PASS；Windows symlink negative test 3 skipped，SQLite historical ZIP SHA 1 skipped。

Base→security-fix candidate delta：

```text
CHANGED_FILES=42
CODE_OR_CONTRACT_FILES=36
MEANINGFUL_SOURCE_FILES_GT_100=NO
RE_FULL_SCAN_REQUIRED=NO
SECURITY_FIX_COMMIT=311d16fc2123a0cce275aa01dc1d077275733cdc
```

CI-equivalent Ruff `E9,F63,F7,F82`、`compileall backend/bridges/scripts` 与 `git diff --check` 均 PASS。完整 Ruff 的既有 I001/SIM/UP/BLE 不属于本轮 authority selector，未做 adjacent cleanup。

## 当前 gate 裁决

```text
REVIEWED_CONFIRMED_P0=0
REVIEWED_CONFIRMED_P1=0
GLOBAL_P0_P1=NOT_FROZEN_UNTIL_REMAINING_HIGH_LEDGER_AND_L3_AUTHORITY
RAW_CRITICAL=79; FP=78; FIXED=1; UNRESOLVED=0
RAW_HIGH=251; EXACT_SOURCE_REVIEWED=23; UNTRIAGED=228
L3_0881=EVIDENCE_BACKED_FALSE_POSITIVE
SMOKE_HIGH_01=LOCALLY_HARDENED_AT_311d16fc; REMOTE_WORKFLOW_REVALIDATION_PENDING
ISSUE_128=OPEN/PENDING_FOLLOWUP
PHASE4_STATUS=PASS
TD_P0_UNRESOLVED=0
TD_P1_UNRESOLVED=0
RELEASE_DRAFTER_STATUS=READY
CANDIDATE_SECURITY_DELTA=LOCAL_TARGETED_PASS; GLOBAL_HIGH_LEDGER_PENDING
PR_MERGED=NO
PRODUCTION=NOT_RUN
RELEASE=NOT_RUN
STOP_REASON=FORBIDDEN_FILE_SCAN_BOUNDARY_VIOLATION
```

## 必须完成的剩余事项

1. 由 Coordinator 明确决定如何处置本轮 forbidden-file scan 边界违规；在此之前不得恢复 Heavy Audit PASS authority。
2. 保持 L3-0362 为单一 L3 authority blocker，不做无证据 PASS；不创建新的 685-unit Full。
3. 若获准在新 authority run 中继续，收敛尚未覆盖的 228 条 Raw High，区分 root-cause duplicate、confirmed FP、lower severity 与真实 unresolved P0/P1。
4. 本地提交继续使用中文 commit message；当前审计文档保持未提交，远端 PR HEAD 仍是旧 SHA。
5. Heavy Audit #1 仍为 PARTIAL/STOPPED 时不 push 本轮 checkpoint、不 merge、不创建 RC/tag/release、不部署。

本检查点的成功标准是：保留全部原始证据、减少真实安全关键路径、提供可复验的源码与测试结果，并明确剩余 authority blocker；不以降低 gate 换取 PASS。

## Clean Evidence Restart V2 权威续页

本节是全新的 clean authority run。它不删除、不美化旧 run 的违规，也不从旧共享 ledger、旧 dedupe 或旧 cluster 文件恢复 disposition。

```text
V2_START_LOCAL_HEAD=311d16fc2123a0cce275aa01dc1d077275733cdc
CLEAN_ROOT=E:\番剧记录\.animemo-security-audit-clean-v2
SANITIZED_BASE_TREE=PASS; TRACKED=816; MATERIALIZED=816
SANITIZED_CANDIDATE_TREE=PASS; TRACKED=823; MATERIALIZED=823
FORBIDDEN_USER_FILE_SCAN=ZERO
FORBIDDEN_USER_FILE_READ=ZERO
FORBIDDEN_USER_FILE_HASH=ZERO
FORBIDDEN_USER_FILE_COPY=ZERO
FORBIDDEN_USER_FILE_LLM_INGESTION=ZERO
ORIGINAL_REPO_SECURITY_SCAN=NO
SAFE_SEARCH_EVENTS=255; NON_APPROVED_ROOT_EVENTS=0
```

V2 使用 Git tree metadata 先过滤路径、再逐 blob 物化 R2 sanitized trees；绝对禁区文件从未进入 tree。全部递归查询只能经过 fail-closed wrapper，255 个 usage record 只指向两个批准的 R2 root。原仓库仅执行 Git metadata/正常 Git 操作，以及 Coordinator 对本文件的定点编辑。

### V2 provenance 与账本

```text
CLEAN_REUSABLE_ARTIFACTS=18
CONTAMINATED_QUARANTINED_ARTIFACTS=4
UNKNOWN_REVERIFY_ARTIFACTS=3

DIRECT_ITEM_LEVEL_RAW=1684
DIRECT_CRITICAL=79
DIRECT_HIGH=240
SMOKE_HIGH=1
V2_HIGH_TRIAGE_ROWS=241
DIRECT_MEDIUM_INVENTORIED=594
DIRECT_LOW_INVENTORIED=754
DIRECT_INFO_INVENTORIED=17

CRITICAL_REVIEWED=79; FP=78; CONFIRMED_P1=1; UNREVIEWED=0
HIGH_REVIEWED=241; FP=227; DUPLICATE=4; P1=1; P2=6; P3=3; UNREVIEWED=0
HISTORICAL_REPORTED_HIGH=251
HISTORICAL_HIGH_DELTA_NOT_REPRODUCED=10
```

历史 High=251 与 clean raw 可重现 High=241 的差额 10 没有被伪造成 finding，也没有通过读取隔离 ledger 恢复。Vercel 仍为 `UNAVAILABLE_COUNT_ONLY_49`；CodeQL 53 条仍为 `UNKNOWN_REVERIFY`，Issue #128 保持 `OPEN/PENDING_FOLLOWUP`。

Coordinator 对 241 条 High 进行了串行合并：所有 raw row 保留 lineage，4 条重复观察不重复计为独立根因。AstrBot Bridge web API finding 重新读取 pinned `AstrBotDevs/AstrBot@ad4fbfa90c` 官方源码：五种 extension method 均依赖 `ScopeDependency("plugin")`，无凭据为 401、scope 不足为 403，认证成功后才把 `auth.username` 绑定到 `PluginRequest` 并调用 handler。因此该项是 fresh `EVIDENCE_BACKED_FALSE_POSITIVE`，不是从旧 disposition 继承，也不增加第二套伪认证边界。

### P0/P1 与修复验证

```text
P0_CONFIRMED=0
P0_FIXED=0
P0_UNRESOLVED=0
P1_CONFIRMED=2
P1_FIXED=2
P1_UNRESOLVED=0
```

两个 P1 根因：

1. performance seed 使用仓库固定 IntegrationConnection HMAC credential；candidate 改为每次 `secrets.token_hex/token_urlsafe`，probe 读取当前连接 secret。
2. official plugin immutability gate 对不受信 git ref 源码执行 `compile/exec`；candidate 删除动态执行与 legacy fallback，只用 AST/literal 读取 registry，并固定调用当前受信候选工作树的 canonical package API。

同一安全提交还修复两个 source-tree link traversal 打包面，以及 `workflow_dispatch` expression 直插 Bash source 的 Smoke P2。

```text
SCRIPT_SCOPED_TESTS=53 PASS; 2 WINDOWS_CAPABILITY_SKIP
DJANGO_SCOPED_TESTS=31 PASS; 3 PLATFORM_CAPABILITY_SKIP
ASTRBOT_BRIDGE_TESTS=55 PASS
TARGETED_L1_L2=5/5 EXPLICIT SANITIZED FILES; 0 FINDINGS
```

Targeted L3 没有被写成 PASS。对 `backend/performance/seed.py` 的一条 operator command 遇到已配置 `local` provider 502；DeepSec CLI 在该命令内部执行两次尝试后失败。Coordinator 没有重发命令、没有扫描第二个 P1 文件、没有换 provider/model、没有增加 timeout：

```text
TARGETED_L3=BLOCKED_EXTERNAL_PROVIDER_502
OPERATOR_COMMANDS=1
DEEPSEC_INTERNAL_ATTEMPTS=2
OPERATOR_RETRY=0
```

### L3 与最终门禁

```text
L3_0200=SATISFIED_BY_SEMANTIC_SUBDIVISION
L3_0362=SECURITY_AUTHORITY_BLOCKER
L3_0362_STREAMING_EQUIVALENCE=FAIL_INSUFFICIENT_EVIDENCE
L3_0362_NEW_EXECUTION_ATTEMPTS=0
L3_AUTHORITY_SATISFIED=684/685
L3_FULL=INCOMPLETE

SMOKE_HIGH_01=LOCALLY_FIXED_REMOTE_EXACT_SHA_AUTHORITY_PENDING
GITHUB_OFFICIAL_STATUS=ALL_SYSTEMS_OPERATIONAL
PR_HEAD_SHA=REMOTE_6d817309_LOCAL_CHECKPOINT_NOT_PUSHED
PR_FAST_GATE=PASS_ONLY_FOR_OLD_REMOTE_SHA
PRE_MERGE_AUTHORITY=PENDING_NEW_EXACT_SHA

FINAL_LEDGER_V2=COMPLETE_FOR_CRITICAL_HIGH_MEDIUM_LOW_INVENTORIED
CANDIDATE_SECURITY_DELTA=LOCAL_TESTS_AND_L1_L2_PASS; TARGETED_L3_EXTERNAL_BLOCKER
HEAVY_SECURITY_AUDIT_1=PARTIAL
SECURITY_FULL_1_SHA=NOT_CREATED
PR_MERGED=NO
PRODUCTION=NOT_RUN
RELEASE=NOT_RUN
```

V2 已把数百条 High 未分流问题收敛为零，并把 candidate 的 P0/P1 unresolved 归零；但 `L3-0362` 仍使 Full authority 停在 684/685，targeted L3 又遇到独立 provider 502。因此禁止制造 Heavy Audit PASS、禁止创建 `SECURITY_FULL_1_SHA`，也禁止 merge、RC、tag、release 或 deploy。后续只允许提交本检查点、重建新 exact-SHA sanitized candidate、执行一次有界 PR 探针并为新 PR HEAD 运行缺失的 exact-SHA gates；任何远端 authority mismatch 都必须保持 BLOCKED。
