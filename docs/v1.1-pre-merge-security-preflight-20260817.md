# AniMemo v1.1 Pre-Merge Security Preflight

Status: PARTIAL — TARGETED L3 PROVIDER UNAVAILABLE

Date: 2026-08-17

Base SHA: `8421d1ed088f21b3f4a6ab190a496e0ab0b0df9f`

Assessed source parent: `4a4a291db72279908cb522f20d543838b973509f`

Branch: `chore/v1.1-pre-rc-consolidation`

This is a branch-delta preflight. It is not Heavy Security Audit #2, does not
create `SECURITY_FULL_1_SHA`, and does not change the existing Heavy Security
Audit #1 authority. No 685-unit Full scan was started.

## Result

```text
PRE_MERGE_SECURITY_PREFLIGHT=PARTIAL
TARGETED_L1=12/12 COMPLETED
TARGETED_L2=12/12 COMPLETED
TARGETED_L3=0/5 COMPLETED; 5 PROVIDER_BLOCKED
NEW_CONFIRMED_P0=0
NEW_CONFIRMED_P1=0
L3_FULL_NOT_STARTED=YES
RE_FULL_SCAN_REQUIRED=NO
```

The partial status is not a product finding and is not promoted to PASS. Five
small, justified L3 units were attempted once each; the configured local
provider returned connection failures or HTTP 502 on both of its internal
attempts. No further retry, provider substitution, timeout increase, fallback,
or Full scan was performed.

## Scope

L1/L2 covered the changed production and authority files for:

- strict managed credential-key parsing and bounded durable writes;
- Updater release-material subprocess isolation;
- official plugin immutable-version enforcement;
- CI release graph authority and AstrBot static validation;
- AstrBot HTTPS/HMAC client and runtime configuration;
- Release Producer, Release Drafter, and native release-note workflows/config.

The five justified L3 attempts were limited to:

- `durability/managed_config.py`;
- `durability/resource_budget.py`;
- `updater/source.py`;
- `scripts/check_official_plugin_immutability.py`;
- `bridges/astrbot_plugin_animemo_bridge/animemo_bridge/client.py`.

Neither the original worktree nor `durability/secret_envelope.py` was scanned,
read, or modified by this preflight.

## Finding correlation

L2 returned zero findings. L1 returned two raw matches in
`.github/workflows/release.yml`; both point to fixed, loopback-only PostgreSQL
qualification credentials on unchanged lines. The branch delta in that file
only deletes the deprecated `dry_run` input/guard. Therefore:

```text
L1_RAW_FINDINGS=2
L1_BRANCH_DELTA_FINDINGS=0
L2_RAW_FINDINGS=0
```

The two raw matches are pre-existing CI fixture values, not reusable production
credentials and not introduced or altered by this branch. They are not deleted
as adjacent cleanup.

## Manual trust-boundary review

- Credential keys now fail closed unless they are canonical URL-safe Base64.
- Durable copy handles short writes and rejects zero, invalid, or over-reported
  progress.
- Anonymous `gh` receives a closed environment; authenticated fallback adds
  only the selected GitHub token.
- The official plugin gate no longer executes a builder loaded from a Git ref
  and carries no private legacy builder.
- CI accepts one release graph version, and Release Producer accepts one
  operation authority input.
- AstrBot accepts one canonical HTTPS origin, exposes no TLS verification
  bypass, and never reflects invalid raw URLs in status.
- Release Drafter is draft-only, performs no checkout in
  `pull_request_target`, and uses job-scoped minimum write permissions.

No compatibility shim, dual security path, credential fallback, release
publication path, or production operation was introduced.

## Supply-chain and GitHub state

- `npm audit --omit=dev --audit-level=high`: 0 vulnerabilities.
- GitHub Dependabot open alerts: 0.
- No dependency manifest or lockfile changed.
- Local `pip-audit` was unavailable and was not installed as an adjacent
  dependency/tooling mutation.
- Code scanning and secret scanning REST endpoints returned GitHub HTTP 503 on
  bounded checks; their current alert counts are therefore not claimed.
- Vercel diff revalidation was not available in the current toolchain and is
  not claimed.

## Test correlation

- Updater full suite: 196 PASS, 4 capability skips.
- Durability/release/CI/plugin authority suite: 90 PASS.
- AstrBot Bridge: 55 PASS; static validator PASS.
- Official plugin gate against exact Base and assessed Head: PASS.
- Release Drafter configuration against upstream v7 schema: PASS.
- CI-equivalent Ruff correctness selection and `git diff --check`: PASS.

PR CI and exact-PR-HEAD pre-merge authority remain required. This document
does not authorize merge while Heavy Security Audit #1 remains partial.

## Preserved authority gap

```text
L3_AUTHORITY=683/685
L3-0200=A_COMPLETED_B_TIMEOUT_C_NOT_STARTED_SYNTHESIS_NOT_STARTED
L3-0362=UNRESOLVED_AUTHORITY_GAP
HEAVY_SECURITY_AUDIT_1=PARTIAL
SECURITY_FULL_1_SHA=NOT_CREATED
```
