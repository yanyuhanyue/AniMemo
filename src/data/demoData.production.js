export const demoEnabled = false;
export const demoAnimeRecords = Object.freeze([]);
export const demoFeaturedColumns = Object.freeze([]);
export const demoUniverseOwners = Object.freeze([]);

export function getDemoUniverseOwner() {
  return null;
}

export function getDemoAuthMessage() {
  return "";
}
