`gh-2.97.0-verified-manifest.json` is the public output of the official Windows
gh 2.97.0 verifying AniMemo v2.0.0-rc.1's original release-manifest.json on
2026-09-15. The command returned zero with exact repository, workflow, issuer,
source and signer constraints. It is a parser replay fixture, not a reusable
verification capability, Linux execution evidence or Candidate/Formal PASS.

The fixture preserves the original bytes, including CRLF line endings. Its
Git binary attribute prevents checkout normalization of this replay input.
